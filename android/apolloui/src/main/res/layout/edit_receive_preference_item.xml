<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        tools:viewBindingIgnore="true"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical">

    <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical">

        <TextView
                android:id="@+id/receive_preference_label"
                style="@style/muun_new_operation_label_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="start"
                android:maxLines="1"
                android:text="@string/edit_receive_preference"
                android:textColor="@color/text_primary_color" />

    </LinearLayout>

    <TextView
            android:id="@+id/receive_preference_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="@dimen/muun_screen_margin"
            android:textSize="16sp"
            android:textColor="@color/text_secondary_color"
            android:gravity="center"
            android:includeFontPadding="false"
            android:drawablePadding="4dp"
            app:drawableEndCompat="@drawable/chevron_down"
            app:drawableTint="@color/blue"
            tools:text="@string/receive_preference_bitcoin" />

</LinearLayout>